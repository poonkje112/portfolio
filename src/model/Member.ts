export class Member {
    
    public name: string;
    public portfolioUrl: string;

    constructor(name: string, portfolioUrl: string) {
        this.name = name;
        this.portfolioUrl = portfolioUrl;
    }
}